[pytest]
addopts = -q
testpaths = tests
markers =
    e2e: marks tests as end-to-end that require a live server
    integration: marks tests as integration tests that require Firebase credentials
    slow: marks tests as slow running
