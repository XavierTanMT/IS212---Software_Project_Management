rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for emulator testing
    // In production, you should have stricter rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
