# Example only. Don't commit real paths with secrets.
# On your machine, copy this to .env and point to your service account JSON path.

FIREBASE_CREDENTIALS_JSON=/absolute/path/to/your/firebase-service-account.json
PORT=5000